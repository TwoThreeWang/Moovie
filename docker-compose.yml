version: '3.8'

services:
  app:
    build: .
    container_name: moovie
    ports:
      - "5007:5007"
    volumes:
      - ./.env:/app/.env
    environment:
      - APP_ENV=production
    restart: unless-stopped
